# UPDATE 1

# Checking alternative architecture

* Fisability check on swping the original WS server with BLE server
  * In this configuration tablet A will use web browser API to implement keyboard that sends the pressed key codes over the BLE client
  * Main question then might be if the esp32 can run BLE server and HID server together and allow both tablets to connect?
* Verify if target table A can be Amazon Fire tablet

